// src/webgpu/types.d.ts
// TypeScript types for WebGPU (for editor support if needed)
// If you have @types/webgpu installed, this file can be omitted.

declare type GPUDevice = import('webgpu-types').GPUDevice;
declare type GPUCanvasContext = import('webgpu-types').GPUCanvasContext;
declare type GPUTextureFormat = import('webgpu-types').GPUTextureFormat;
